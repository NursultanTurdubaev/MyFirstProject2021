package javaOperators;

public class LogicalOperators {
    public static void main(String[] args) {
        // && ->  it is pronounced double ampersand. it takes 2 conditions and only returns true when both conditions are true
        //                   true        false
        boolean ampersand1 = 10 > 8 && 89 < 16;
        System.out.println(ampersand1);
        boolean ampersand2 = 90==90 && 78>39;
        System.out.println(ampersand2);

        // || means either one of the conditions must be true to return true otherwise false
//  ! before an operator makes it opposite. ! means not







    }
}
